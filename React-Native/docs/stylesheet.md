```javascript
const styles = StyleSheet.create({
    headerContainer: {
        display: 'flex',
        justifyContent: 'flex-start',
        alignItems: 'center',
        marginTop: 55
    },
    header: {
        fontSize: 20,
        fontWeight: 'bold'
    }
});
```